extends RefCounted
## 高度分层（M1 内容纪律）：任何单位 / 特效 / 地貌都必须落在下列某一层，
## 禁止在各自脚本里就地写任意 y —— 想加一个新高度，先在这里登记。
##
## 【为什么需要这份表】正交相机下「高度」是**纯表现层**的独立维度，不参与任何 XZ 判定：
##
## - **68° 斜俯视**（默认机位，F1）：高度 h 把物体在屏幕上抬高 0.447·h（= cos68°/sin68° 的
##   投影压缩比，正交投影下与距离无关），而它的 XZ 落点不动；同时方向光（-52°/-28°）把
##   影子推到 0.367·h 右 / 0.690·h 远的位置 → 屏幕上物体与影子拉开 **0.40·h**。
##   → 层次感 = 视差 + 影子错位，两者叠加；
## - **90° 纯俯视**（F2）：高度几乎不产生屏幕位移（0.026·h），物体正好压在自己的 XZ 上，
##   → 层次**只**靠影子错位读出，此时 h 越大「浮空感」越强（俯视图里读高度就是读影子）；
## - **等距**（F5）：两个分量各占一半。
##
## 结论：想让"飞得更高"被看出来，靠的是**影子**，不是机位；而改 y 不影响任何 XZ 判定，
## 所以高度是纯表现层的自由变量（两作并排试玩的手感对照不会被破坏）。
##
## 【相对 M0 的目标变化】把**空域整体拉高**，在海面/地面与空域之间空出真正塞得下内容的
## 夹层：M1 要放地面炮台（贴海）、掠海单位（LOW）、敌机主群（AIR）、精英/大型单位（HIGH）、
## 云层（CLOUD_*）。M0 只有「海面 0 / 玩家 4.5 / 云 16~20」三层，中间全空、云又贴得太近。
##
## 【语义】每条常量是该层**内容原点的 y**：
## - 体素单位由 `voxel_model.gd` 烘成 **AABB 居中**，所以单位的"原点"= 包围盒中心；
## - 瓦片是**底面锚点**（原点是底面，`tiles.gd::get_tile` 的 lift 已把底面对到 y=0）。

const SEA := 0.30          # 海面：瓦片水面层顶面（底面锚点 0 + 1 体素 × 0.3）。贴海特效的贴合面
const GROUND := 0.30       # 地面单位（炮台 / 幸存者等）的**底面**贴合高度 —— 与 SEA 同值但单独命名：
                           # 将来出现"岛上着陆面"时改这一条即可，不必去动海面
const LOW := 3.4           # 低空层：掠海单位、水面浪花 / 溅射特效
const AIR := 7.5           # 主力空域：玩家机与敌机主群（M0 = 4.5，+67%）
const HIGH := 11.5         # 高空层：精英 / 大型单位（M3 起方舟 Boss 刻意不放这层——
                           # Boss 战必须与玩家同平面才读得清，见 waves.gd 的 BOSS 条目）
const CLOUD_LO := 22.0     # 云层下限（M0 = 16）
const CLOUD_HI := 30.0     # 云层上限（M0 = 20）


## 层名 → 高度（`waves.gd` 的 `layer` 字段就是这些名字）。**战斗层不许自己写 y**：
## 加一个新高度先在上面登记常量、再在这里补一行，全工程只有这一处映射。
static func of(name: String) -> float:
	match name:
		"SEA":
			return SEA
		"GROUND":
			return GROUND
		"LOW":
			return LOW
		"AIR":
			return AIR
		"HIGH":
			return HIGH
		"CLOUD_LO":
			return CLOUD_LO
		"CLOUD_HI":
			return CLOUD_HI
	push_warning("altitude.gd：未登记的层名「%s」，回退 AIR" % name)
	return AIR
